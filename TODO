- add support for other distros (deb-based, gentoo)

- support for different distro-dependent RPM binaries
  (see http://bugzilla.openvirtuozzo.org/27)

- process everything in VPS context, not in host context
  (requires patched rpm and yum)
